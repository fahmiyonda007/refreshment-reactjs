const columns = [
  {
    name: 'email',
    selector: (row) => row.email,
    sortable: true,
  },
  {
    name: 'name',
    selector: (row) => row.name,
    sortable: true,
  },
]

export default columns
